# Serve the homepage at the root URL (the main file is instyle-painting.html, not index.html)
[[redirects]]
  from = "/"
  to = "/instyle-painting.html"
  status = 200

# Serverless functions (weekly business summary)
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
